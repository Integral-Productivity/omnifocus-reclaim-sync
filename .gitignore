# macOS
.DS_Store

# Local Reclaim MCP session state (per-session, not part of the plugin)
.claude/.reclaim-session.json

# Plugin support for compound engineering with Claude
.compound-engineering/*.local.yaml
